import React from 'react';
import { IReview, IReviewRequest, ITask } from '../../interfaces/interfaces';

interface ICreateDisputProps {
  task: ITask | null;
  request: IReviewRequest | null;
  review: IReview | null;
}

export const FormCreateDispute: React.FC = () => {
  return <></>;
};
